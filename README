Proxies requests to a given target (or sendgrid.net by default) over HTTP but also directly serves an apple-app-site-association file if requested directly.

Examples (env.TARGET == https://sendgrid.net)
curl http://track.betterworks.com/apple-app-site-association
curl http://track.betterworks.com/img/login-logo.png

Use this project if you ever want to enable email deep linking in iOS and utilize email service providers' native link tracking mechanism.
